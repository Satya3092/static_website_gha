name: output-demo
in: push
job:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step one 
        id: step1
        run: echo "version 0.0.1" >> $GITHUB_OUTPUT
      - name: step two
        run: echo "the version is ${{ step.step1.outputs.version }} "
